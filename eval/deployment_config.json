{
  "protocol_version": "2.0-RFC",
  "governor_id": "stp-kernel-production-001",
  "configuration": {
    "bridge_tax_config": {
      "drift_tolerance": 0.05,
      "insolvency_limit": 0.15,
      "max_negotiation_turns": 2,
      "strict_mode": true,
      "return_policy": {
        "kind": "phased",
        "lambda": 0.25
      }
    },
    "auditor_config": {
      "combination_rule": "max",
      "distance_metric": "cosine"
    }
  }
}
